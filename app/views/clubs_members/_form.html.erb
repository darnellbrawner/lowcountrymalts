<%= form_for(@clubs_member) do |f| %>
  <% if @clubs_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clubs_member.errors.count, "error") %> prohibited this clubs_member from being saved:</h2>

      <ul>
      <% @clubs_member.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :club_id %><br />
    <%= f.number_field :club_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br />
    <%= f.number_field :member_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
